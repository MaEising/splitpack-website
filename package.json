{
  "name": "splitpack-website",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker/test/*.test.mjs",
    "worker:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
